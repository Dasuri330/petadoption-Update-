<?php
/*
  include 'database.php';

  //assume that there is delete button and deletebutton id in html
  if (isset($_GET['deleteid'])){ 
    $id=$_GET['deleteid'];

    $sql="delete from 'petadoptionlogin' where id=$id";
    $result=mysqli_query($con,$sql);
    if($result){
        header('location:admin.php')// assume that there is a page for admin where he can delete the data
    }else{
        die(mysqli_error($con));
    }
  }
*/
?>

